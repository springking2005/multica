# 波笛智能体用户操作手册 - 审核意见

**审核日期：** 2026-05-11
**审核人：** Code Reviewer
**手册文件：** /home/ocx/project/multica/repo/bodiagent/docs/用户操作手册.md
**手册版本：** 1.0

---

## 审核结论

**裁决：REQUEST CHANGES**（存在 P0 事实错误，必须修正）

**总问题数：** 18
- P0（事实错误/阻断性问题）：10
- P1（遗漏/误导）：6
- P2（措辞/改进）：2

---

## 逐章审核明细

### 第 1 章：欢迎与概述

**结论：通过。** 核心概念定义准确，与项目模型（accounts/models.py, issues/models.py, agents/models.py, autopilots/models.py）一致。术语表（第 11 章）与代码常量命名对齐良好。

**NOTES：**
- [P1] **遗漏** - 第 1.2 节未提及 **Chat（聊天会话）** 功能。chat/models.py 中有 ChatSession 和 ChatMessage 模型，chat/views.py 中有完整的 API 端点（`/api/sessions/`）。这是完整缺失的功能模块。

---

### 第 2 章：快速入门

**[P0] 第 2.1 节 - 注册流程描述有事实错误（第 67-75 行）**

- **问题：** 手册将邮箱验证码描述为注册流程的一部分（"如果使用邮箱验证码方式，输入收到的 6 位验证码"），但实际存在**两条独立的认证路径**：
  1. 传统注册：`POST /auth/register`（accounts/serializers.py: RegisterSerializer，字段：email + name + password），无需验证码
  2. 验证码登录：`POST /auth/send-code` → `POST /auth/verify-code`（accounts/views.py: verify_code），通过验证码创建/查找用户并返回 JWT
- **证据：** accounts/serializers.py:53-67 RegisterSerializer 无 verification code 字段；frontend/templates/accounts/register.html 表单也无验证码字段
- **建议：** 分开描述两种注册/登录方式，明确区分"邮箱密码注册"和"验证码登录"

**[P0] 第 2.3 节 - 工作区创建描述依赖不存在的 UI（第 96-107 行）**

- **问题：** 手册描述"点击左侧边栏底部的设置图标，进入设置页面。在工作区管理区域，点击创建工作区"。但实际 settings/profile.html 模板**仅有**个人信息、偏好设置、通知偏好三个区域，**没有**工作区管理 CRUD UI
- **证据：** frontend/templates/settings/profile.html（仅 120 行，无 workspace 管理表单）
- **建议：** 标注工作区管理当前通过 API 操作（`POST /api/workspaces/`），Web UI 正在开发中

**[P0] 第 2.4 节 - 团队成员邀请依赖不存在的 UI（第 114-126 行）**

- **问题：** 同样描述操作在设置页面完成，但 profile.html 无成员邀请表单
- **证据：** 同上，profile.html 不含 invitation/member 管理区域
- **建议：** 标注通过 API 操作（`POST /api/workspaces/{id}/members/`）

---

### 第 3 章：工作区管理

**[P0] 第 3.2-3.5 节 - 整个工作区管理章节依赖不存在的 UI（第 172-220 行）**

- **问题：** 所有工作区编辑、成员管理、退出、删除操作均描述为在"设置页面"完成，但 settings/profile.html 不包含这些功能
- **证据：** profile.html 仅包含个人资料编辑、主题/语言偏好、通知偏好；API 端点存在（WorkspaceViewSet 支持 CRUD），但 Web UI 尚未实现
- **建议：** (1) 在章首添加说明"当前版本部分操作通过 API 完成，Web UI 正在完善中"；(2) 为每项操作注明对应的 API 端点

---

### 第 4 章：Issue 管理

**结论：大部分通过。** Issue 状态、优先级、看板布局、详情页描述均与代码一致。

**已验证正确的内容：**
- 4.1 节：7 种 Issue 状态与 issues/models.py:Issue.Status ✅
- 4.2 节：5 种优先级与 Issue.Priority ✅
- 4.3 节：5 列看板布局与 board.html 完全一致 ✅
- 4.7 节：评论 API 路径 `/api/issues/{issue_id}/comments/` 与 issues/urls.py 一致 ✅
- 4.11 节：父子 Issue（parent_issue FK）与 Issue 模型一致 ✅
- 4.12 节：3 种依赖类型（blocks/blocked_by/related）与 IssueDependency.Type 一致 ✅

**[P0] 第 4.4 节 - 快速创建按钮不可用（第 282-287 行）**

- **问题：** 手册描述"在看板任意列的顶部点击 + 按钮。在弹出的输入框中输入标题。按回车提交。" 但实际 board.html 中每个列的 `+` 按钮**没有 HTMX 属性或事件处理**，点击无反应
- **证据：** board.html:33-37 的 `<button class="kanban-add-btn">` 仅有 SVG 图标，无 hx-* 属性
- **建议：** 标注"快速创建（开发中）"或移除该描述

**[P0] 第 4.9 节 - 标签管理依赖不存在的 UI（第 361-372 行）**

- **问题：** "进入设置页面，找到标签管理区域" - profile.html 无标签管理
- **证据：** profile.html 无 label CRUD 表单
- **建议：** 标注通过 API 操作（`POST/GET /api/labels/`）

---

### 第 5 章：Agent 管理

**结论：部分通过。** Agent 列表和创建功能在 UI 中已实现。

**已验证正确的内容：**
- 5.2 节：11 种 AI 提供商与 Agent.Provider 完全一致 ✅
- 5.3 节：创建 Agent 表单字段（名称、描述、提供商、模型、指令）与 agents/list.html:52-99 一致 ✅
- 5.4 节：卡片网格布局、每张卡片包含名称首字母图标、提供商/模型、状态标签、最大并发数、创建时间，与 agents/list.html:123-146 渲染逻辑一致 ✅
- 5.8 节：归档/恢复与 AgentViewSet.archive/restore 方法一致 ✅
- 5.9 节：取消任务与 AgentViewSet.cancel_tasks 方法一致 ✅

**[P0] 第 5.5 节 - "点击目标智能体卡片...进入编辑模式" 无法实现（第 477-494 行）**

- **问题：** agents/list.html 渲染的是纯 HTML 卡片，**没有**链接或导航到编辑页。点击卡片不会进入任何编辑界面
- **证据：** agents/list.html:128 的卡片仅是 `<div>`，无 hx-* 或链接属性
- **建议：** 标注当前 Agent 编辑通过 API 完成（`PATCH /api/agents/{id}/`），完整编辑 UI 开发中

**[P0] 第 5.6-5.7 节 - 技能管理无对应 UI（第 496-526 行）**

- **问题：** 技能绑定、创建、文件上传描述为 UI 操作，但无任何技能管理模板
- **证据：** 无 skill 相关的 Django 模板文件
- **建议：** 标注通过 API 操作（`POST/GET /api/skills/`，`PUT /api/agents/{id}/skills/`）

---

### 第 6 章：项目管理

**[P0] 整个第 6 章 - 项目模块无前端路由（第 568-623 行）**

- **问题：** 
  1. 侧边栏指向 `/projects` 但 bodiagent/urls.py **没有** `/projects` 前端路由
  2. 无 project 相关的 Django 模板文件（无 list.html, detail.html 等）
- **证据：** `grep -rn "/projects" bodiagent/urls.py` 无输出；find frontend/templates 无 project 目录
- **建议：** 章首添加醒目说明"项目管理功能当前仅通过 API 可用（`/api/projects/`），Web 界面开发中"

---

### 第 7 章：收件箱与通知

**结论：通过。** 收件箱功能在 UI 中完整实现。

**已验证正确的内容：**
- 7.1 节：页面布局描述与 inbox/list.html 完全一致（标题栏、全部已读/归档已读按钮、通知列表布局）✅
- 7.2 节：通知类型与 InboxItem.Type 完全一致 ✅
- 7.3 节：点击通知跳转到 Issue 详情页，与 inbox/list.html:70 的 `<a href="' + issueLink + '">` 一致 ✅
- 7.5 节：归档操作（全部已读、归档已读）与 inbox/list.html:14-24 的按钮一致 ✅

---

### 第 8 章：自动化（Autopilot）

**[P0] 整个第 8 章 - 自动化模块无前端模板（第 703-799 行）**

- **问题：** 所有 Autopilot 操作描述为 UI 操作，但**没有任何** autopilot 相关的 Django 模板文件
- **证据：** find frontend/templates 无 autopilot 目录或文件
- **建议：** 章首添加说明"Autopilot 功能当前通过 API 管理（`/api/autopilots/`）"

**已验证正确的 API 相关内容：**
- 8.1 节：三种触发方式（定时/Webhook/API）与 AutopilotTrigger.Kind 一致 ✅
- 8.2 节：两种执行模式（create_issue/run_only）与 Autopilot.ExecutionMode 一致 ✅
- 8.5 节：运行状态与 AutopilotRun.Status 一致 ✅

---

### 第 9 章：个人设置

**结论：部分通过。** 个人信息和偏好设置在 UI 中已实现。

**已验证正确的内容：**
- 9.1 节：个人信息表单（姓名、邮箱、头像 URL）与 profile.html:13-40 一致 ✅
- 9.4 节：通知偏好 5 个复选框与 NotificationPreference 模型字段一致 ✅

**[P1] 第 9.2-9.3 节 - 主题和语言设置部分实现（第 829-853 行）**

- **问题：** 
  - 主题：手动描述"选择后即时生效"，实际通过 Alpine.js + localStorage 实现，但无 Tailwind darkMode 配置，深色模式 CSS 变量未在 base.html 设置
  - 语言：profile.html:52-55 有语言选择下拉框但**无 hx-* 或 form 提交逻辑**，选择后不会保存
- **证据：** profile.html:52-55 的 `<select>` 无 hx-* 属性，无 form 包裹
- **建议：** 标注语言切换为 UI 预览（后端存储通过 API 实现）

**[P0] 第 9.5 节 - 个人访问令牌管理无对应 UI（第 870-885 行）**

- **问题：** "进入设置页面。找到个人访问令牌区域。" - profile.html 无令牌管理区域
- **证据：** profile.html 仅含个人信息、偏好、通知三个卡片
- **建议：** 标注通过 API 操作（`POST /api/tokens/`，`DELETE /api/tokens/{id}/`）

---

### 第 10 章：常见问题 FAQ

**结论：通过。** FAQ 内容总体上准确，与代码行为一致。

**已验证正确的内容：**
- 10.4 节：Agent 不执行任务的原因（无 Daemon、Daemon 离线、未分配、并发上限）与 agents/models.py:Task 和 Agent 模型一致 ✅
- 10.1 节：暂无自助找回密码 - 与当前代码一致（无密码重置功能）✅

**[P1] 第 10.2 节 "如何切换工作区"（第 914-915 行）**

- **问题：** "在侧边栏顶部显示当前工作区名称。点击可切换工作区" - sidebar.html:13-21 仅显示工作区名称的**纯文本** `<p>`，不可点击，无切换功能
- **证据：** sidebar.html:15-21 `<p class="mt-1 text-sm font-semibold text-gray-900 truncate">` 非可点击元素
- **建议：** 标注"工作区切换功能正在开发中"

---

### 第 11 章：附录：术语表

**结论：通过。** 术语定义准确，中英文对照与代码命名和注释一致。

---

## 遗漏功能清单

以下功能在项目代码中完整实现（有模型 + 视图 + API），但手册中**完全未提及**：

| 功能 | 代码位置 | 影响 |
|------|---------|------|
| **Chat（聊天会话）** | chat/models.py, chat/views.py, `/api/sessions/` | 用户无法了解可与 Agent 进行对话式交互 |
| **WebSocket 实时通信** | bodiagent/asgi.py, CHANNEL_LAYERS 配置 | 用户不知道支持实时更新 |
| **Pins（固定项）** | inbox/models.py:Pin, `/api/pins/` | 用户不知道可以固定 Issue/Project |
| **Activities（活动日志）** | inbox/models.py:Activity, `/api/activities/` | 虽然 Issue 详情页引用了活动时间线（`/api/activity/`），但未独立讲解 |
| **Task（任务队列）** | agents/models.py:Task, `/api/tasks/` | Agent 执行任务的底层机制未解释 |
| **Daemon（守护进程管理）** | accounts/models.py:Daemon, `/api/daemons/` | 关键基础设施仅在第 1 章术语表中一笔带过 |

---

## 术语检查

| 术语 | 规范要求 | 手册使用 | 结论 |
|------|---------|---------|------|
| Agent | 智能体 | 全文使用"智能体" | ✅ 通过 |
| Issue | 保留英文 | 全文使用"Issue" | ✅ 通过 |
| Workspace | 工作区 | 全文使用"工作区" | ✅ 通过 |
| Daemon | 守护进程 | 术语表中定义，正文偶用 Daemon | ⚠️ P2: Section 5.3 "Agent 会自动绑定到可用的 Daemon" 宜用"守护进程" |
| Autopilot | 自动化/自动化调度 | 混用"自动化"、"Autopilot"、"自动化调度" | ⚠️ P2: 建议统一为"自动化（Autopilot）" |

---

## 正面观察

以下是手册中做得好的方面，值得肯定：

1. **API 路径准确：** 所有引用 API 端点的描述（如 `/api/issues/{id}/comments/`）与 URL 配置完全匹配
2. **模型字段对齐：** Issue 状态、优先级、Agent 提供商、项目状态等枚举值与代码定义一致
3. **看板描述精准：** 第 4.3 节对看板的 5 列布局、状态颜色、卡片内容的描述与 board.html 完全吻合
4. **收件箱功能完整：** 第 7 章对收件箱的布局、通知类型、操作方式的描述与 inbox/list.html 实际行为一致
5. **Agent 创建表单准确：** 第 5.3 节描述的创建表单字段与 agents/list.html 中的 Alpine.js 模态表单一致
6. **术语表系统规范：** 第 11 章的中英文对照、状态缩写、角色权限表为整个文档提供了统一的参考标准
7. **开发环境提示到位：** 验证码 `888888` 的说明正确引用自 settings_dev.py:31
8. **邀请过期时间准确：** 7 天有效期与 accounts/models.py:18-19 的 `default_invitation_expiry` 一致
